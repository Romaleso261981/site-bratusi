import { Button, Group } from '@mantine/core';

const ButtonSwitcher = () => {
  return (
    <Group justify="center">
      <Button variant="default">English</Button>
      <Button variant="light">Ukraine</Button>
    </Group>
  );
};

export default ButtonSwitcher;
